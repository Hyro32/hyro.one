---
const { post } = Astro.props;
---

<a
  href={`/blog/${post.slug}`}
  class="w-3/4 p-3 text-center rounded-md bg-neutral-800 duration-300 hover:scale-105 max-sm:w-[90%]"
>
  <h2 class="text-xl font-bold">{post.data.title}</h2>
  <p class="text-gray-500">{post.data.description}</p>
  <p class="text-gray-500 text-xs">{post.data.pubDate.toLocaleDateString()}</p>
</a>
